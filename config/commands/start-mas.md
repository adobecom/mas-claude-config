---
name: start-mas
description: Start MAS development environment (AEM server + proxy)
triggers:
  - "start mas"
  - "start the server"
  - "start dev server"
  - "start aem"
  - "run the server"
---

Run the following command to start both the AEM server and the proxy:

```bash
npm run studio
```
